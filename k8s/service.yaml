apiVersion: v1
kind: Service
metadata:
    name: task-app-service
    namespace: task-app
spec:
    type: LoadBalancer
    selector:
        app: task-app
    ports:
        - port: 80
          targetPort: 80
          protocol: TCP
          name: http
